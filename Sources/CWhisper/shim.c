// Intentionally empty. This target only exposes the whisper.cpp C headers
// (see include/module.modulemap); the prebuilt static libraries in vendor/lib
// are linked by the Meetily executable target.
